#include <stdio.h> //this is the headerfile, which contains the printf and the scanf
int main () //this is the main function
{
    int i=10; //this is the given value of variable i, which is 10
    if(1<15){ //if i is less to 15...
        printf("i is smaller than 15"); //show to user i is smaller than 15
        }

        if(i<12){ //if i is less to 12...
        printf("\ni is smaller than 12 too"); //show to user i is smaller than 12 too
        }

        else
            printf("\ni is greater than 15"); //show to user i is greater than 15
    }


